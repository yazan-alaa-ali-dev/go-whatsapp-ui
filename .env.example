# Target of the vite dev proxy (see vite.config.ts): /api/* and /health are
# forwarded here during `npm run dev`. Build-time only — nothing under src/
# reads it, so it never lands in the bundle.
VITE_DEFAULT_SERVER_URL=http://localhost:3000
